<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.happy.trans">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Change_Lang">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Translation"></activity>
        <activity android:name=".List"></activity>
        <activity android:name=".Select_Function"></activity>
        <activity android:name=".Setting"></activity>
        <activity android:name=".Highlight"></activity>
        <activity android:name=".Medicine"></activity>
        <activity android:name=".SelectSearch"></activity>
        <activity android:name=".WordSearch"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>